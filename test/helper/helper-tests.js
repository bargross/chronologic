import { replaceMultipleValues } from '../../chronologic/helper/helper';


describe('replaceMultipleValues', () => {
    const values = [

    ];

    describe('when parameter [values] is invalid', () => {

        it('should throw an error', () => {
            expect(replaceMultipleValues())
        })
    });
});